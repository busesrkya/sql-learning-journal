-- Topic: Basic SELECT queries

-- Select every column and row
SELECT * FROM students;

-- Select only specific columns
SELECT first_name, last_name FROM students;

-- Select every column from the grades table
SELECT * FROM grades;
